name: atelier
description: Personal fashion intelligence — visual app, honest data only.
publish_to: 'none'
version: 0.0.0+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  yaml: ^3.1.3
  device_info_plus: ^13.2.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/registry.yaml
