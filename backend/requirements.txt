fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pytest>=8,<9
httpx>=0.27,<1
python-multipart>=0.0.9,<1
PyYAML>=6,<7
# Phase 1 inference stack — verified 2026-08-14 (license, activity, CPU fit).
mediapipe==1.0.1
# Headless build: the backend never opens a window and needs no libGL/X11.
opencv-python-headless>=4.10,<6
